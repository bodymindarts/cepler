jobs:
{{#each jobs ~}}
{{> job this }}
{{/each ~}}
resources:
{{#each cepler_resources ~}}
{{> cepler_resource this }}
{{/each ~}}
resource_types:
- name: cepler
  type: registry-image
  source:
    repository: bodymindarts/cepler
    tag: latest
