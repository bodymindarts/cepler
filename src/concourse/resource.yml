- name: {{name}}
  type: git
  source:
    uri: {{repo_uri}}
{{~#if paths}}
    paths:
{{~#each paths}}
    - {{this}}
{{~/each}}{{~/if}}
    branch: {{branch}}
    private_key: {{github_private_key}}
